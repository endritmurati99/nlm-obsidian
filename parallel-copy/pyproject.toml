[project]
name = "nlm-obsidian-parallel-copy"
version = "0.1.0"
description = "Parallel copy of the NLM + Obsidian research pipeline"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
